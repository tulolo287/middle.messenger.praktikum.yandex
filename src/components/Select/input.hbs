<select class="input" type={{type}} value={{#if value}} {{value}} {{else}} '' {{/if}}  name={{name}} placeholder={{placeholder}} required >
{{#each options}}
<option value={{this.id}}>{{this.title}}</option>
{{/each}}
</select>
